import styled from "styled-components";
import { Container } from "../Container";

export const HomeServicesContainer = styled(Container)`
    display: flex;
    flex-direction: column;
    gap: 2em;
    align-items: center;
    text-align: center;
`;
